# EU4dll Project Context for Hermes Agent

このプロジェクトでは、`memory-bank/` を永続的なプロジェクトコンテキストとして使用します。
タスク開始時は、以下の手順で現在の状況を確認してください。

## 作業開始時の必須手順

1. リポジトリルートの `.clinerules` を読む。
2. `memory-bank/` 内の主要ドキュメントを読む。
   - `01_project_overview.md`
   - `02_architecture.md`
   - `03_development_guide.md`
   - `04_testing_guide.md`
   - `05_coding_guidelines.md`
   - `06_git_workflow.md`
   - `07_ai_workflow.md`
   - `07_commit_message_guidelines.md`
   - `08_conversation_guidelines.md`
   - `linux_development_notes.md`
3. タスクがバイナリ解析、Linuxパッチ、または手動テストに関係する場合は、必要に応じて以下も読む。
   - `memory-bank/TODO.txt`
   - `memory-bank/binary_analysis_guide.md`
   - `memory-bank/details/linux_patching_details.md`
   - `memory-bank/MANUAL-TEST.txt`
4. `git status --short --branch`、対象ファイル、関連するシンボルと利用箇所を確認する。
5. 変更後は関連テストまたはビルドを実行し、結果を確認する。

## プロジェクト概要

- Europa Universalis IVで日本語などの2バイト文字を表示するためのDLL。
- 旧C++実装からD言語実装への移行中。
- Linuxでは共有ライブラリを`LD_PRELOAD`でロードし、`mprotect`を使用して実行時パッチを適用する。
- ライセンスはMIT。
- upstream/fork元: `matanki-saito/EU4dll`
- リポジトリ: `hangingman/EU4dll`

## 現在の実装構成

- `source/`: D言語の実装。
- `source/plugin/`: EU4のフック、バイトパターン検索、パッチ、テキスト・ローカライズ処理。
- `tests/`: ユニットテストとLinuxの`.so`ロード検証用PoC。
- `submodules/`: 翻訳ソースなどのGitサブモジュール。
- `memory-bank/`: プロジェクト仕様、開発・テスト手順、解析メモ。
- `Makefile`: ビルド、テスト、PoC、EU4実行、翻訳配置の入口。
- `dub.json`: DUB構成。LDCを使用する。

### 技術上の重要事項

- 標準コンパイラは`ldc2`。DMDを前提にしない。
- `make all`: `dub build --compiler=ldc2`。
- `make test`: ユニットテストをビルドし、`--threads=1`で実行する。
- Linux共有ライブラリのPIC要件により、インラインアセンブリのグローバル変数アクセスは`[RIP + symbol]`を使用する。
- LDCの`MOVSXD`互換性問題は、`shl`と`sar`による手動符号拡張で回避する。
- Intel/DMD互換のメモリオペランドでは、必要に応じて`byte ptr [...]`のようにサイズを明示する。
- `ScopedPatch`は値型`struct`としてRAIIライフサイクルを管理する。
- メモリパッチはW^Xを考慮し、書き込み時と実行時のページ権限を分ける。
- EU4本体のアドレスを推測せず、バイトパターン検索とバイナリ解析の結果に基づいて扱う。
- ゲーム実行を伴う変更では、パッチ失敗時のクラッシュ回避とログ確認を優先する。

## 標準コマンド

```sh
make all
make test
make clean
make hijack
make run
make translations
```

`make run`と手動テストはEU4のインストール、翻訳MOD、`LD_PRELOAD`環境に依存する。実行できない場合は、未検証のまま成功と報告しない。

## AI協業ルール

- 回答は日本語で、簡潔かつ事実ベースにする。
- 変更前に対象コードと関連箇所を調査する。
- 最小差分を優先し、無関係なリファクタリングを行わない。
- コード変更の意図と目的を明確にする。
- デバッグではKT法のIS/IS NOT分析を使用し、仮説と検証結果を記録する。
- 実装後はビルド・テスト・必要な実機または手動確認を自律的に完了する。
- 詳細な履歴は`memory-bank/details/`に置き、概要文書を肥大化させない。
- `memory-bank/`を更新した場合は、既存のGit運用ルールとユーザーの指示を確認してからコミット・プッシュを行う。明示的な依頼なしにコミット、プッシュ、履歴書き換えを行わない。
- リポジトリに個人のホームディレクトリをハードコードしない。パスは相対パスまたは実行時に解決する。
- `.env`や認証情報を読まない。

## 現在の主な課題

- Ghidraを用いた「テキスト処理ループ2」の特定。
- バイナリ解析結果と`main_text.d`などの実装済みフックアドレスの同期。
- `LD_PRELOAD`で実際にEU4を起動し、パッチ適用と日本語表示を検証する。
- 残存するC++/アセンブリ機能のD言語代替確認。
- D言語テストカバレッジとCI/CD対応の拡充。

## 注意

- `memory-bank/TODO.txt`には過去の重複項目や、実装済み項目と未検証項目が混在している。チェック状態をそのまま鵜呑みにせず、コード・テスト・実機結果で確認する。
- Ghidra関連のZIPや生成物などの未追跡ファイルは、依頼なしに削除・追加・コミットしない。
- 現在のブランチ運用・コミット方針は、リポジトリのルールとユーザーの最新指示を優先する。

## 参照

- 開発・テスト手順: `memory-bank/03_development_guide.md`, `memory-bank/04_testing_guide.md`
- Linuxパッチ: `memory-bank/details/linux_patching_details.md`
- バイナリ解析: `memory-bank/binary_analysis_guide.md`
- 手動テスト: `memory-bank/MANUAL-TEST.txt`
- コーディング・Git・AI協業ルール: `memory-bank/05_coding_guidelines.md`〜`08_conversation_guidelines.md`
