# テストガイド

このドキュメントでは、プロジェクトのテストの実行方法について説明します。

## テストの実行

プロジェクトのルートディレクトリで以下のコマンドを実行すると、Dubがテストをビルドし、実行します。

```bash
make test
```

### テスト結果

上記コマンド実行後、以下のような成功メッセージが表示されれば、テストは成功です。

```
Summary: 13 passed, 0 failed in 21 ms
```
