description = "Issueを推進、解決します。"
prompt = """
以下のIssueの内容を解決するための作業プランを提案し、推進してください。

Issue：
```
<ここにIssueの内容を貼り付け>
```

* **最優先かつ必須のタスクとして**、`github.get_issue` ツールを使用してIssueの詳細を取得してください。**ユーザーからの指示の直後に、他のどの応答よりも先に実行する必要があります。**
* ローカルgitブランチを確認し、プロジェクトの方式に従い適切な作業ブランチを作成してください。**この確認には `run_shell_command 'git branch'` を使用してください。**
"""
